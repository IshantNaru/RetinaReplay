# RetinaReplay — Requirements
# Python 3.9+
#
# ══════════════════════════════════════════════════════════════════════════════
# IMPORTANT: Read the full install notes before running pip install -r
# The correct install order matters. See README.md Step 2 for full instructions.
# ══════════════════════════════════════════════════════════════════════════════
#
# STEP 1 — Install PyTorch FIRST with the correct CUDA version for your GPU.
#           Check your CUDA version: nvidia-smi (top-right of output)
#           Then install from https://pytorch.org/get-started
#
#   CUDA 12.8 (RTX 40xx series):
#     pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
#
#   CUDA 12.1:
#     pip install torch==2.5.1+cu121 torchvision==0.20.1+cu121 \
#       --index-url https://download.pytorch.org/whl/cu121
#
#   CPU only (reconstruction will be very slow):
#     pip install torch torchvision
#
# STEP 2 — Downgrade setuptools BEFORE installing other packages.
#           Required for CLIP installation compatibility:
#     pip install setuptools==80.10.2
#
# STEP 3 — Install this requirements.txt:
#     pip install -r requirements.txt
#
# STEP 4 — Install packages requiring manual setup (see INSTALL NOTES below)

# ── Core ML ───────────────────────────────────────────────────────────────────
numpy==2.2.6
scikit-learn==1.7.1
scipy==1.16.1

# ── Deep Learning ─────────────────────────────────────────────────────────────
# NOTE: pytorch-lightning 1.9.4 specifically — API is not compatible with 2.x
pytorch-lightning==1.9.4
omegaconf==2.3.0
einops==0.8.1
timm==1.0.19
kornia==0.8.1
kornia_rs==0.1.9
fairscale==0.4.13
transformers==4.46.3
safetensors==0.6.2
diffusers==0.34.0
lpips==0.1.4

# ── fMRI / Neuroscience ───────────────────────────────────────────────────────
himalaya==0.4.6
nibabel==5.3.2

# ── Computer Vision & Image ───────────────────────────────────────────────────
Pillow==11.3.0
opencv-python==4.12.0.88
matplotlib==3.10.5
invisible-watermark==0.2.0

# ── Data & I/O ────────────────────────────────────────────────────────────────
pandas==2.3.1
h5py==3.14.0
dask==2025.7.0
pycocotools==2.0.10
tqdm==4.67.1
ftfy==6.3.1
regex==2025.7.34
PyYAML==6.0.2
requests==2.32.4

# ── HuggingFace ───────────────────────────────────────────────────────────────
huggingface-hub==0.34.4
hf_xet                        # Optional but recommended — faster HF downloads

# ══════════════════════════════════════════════════════════════════════════════
# INSTALL NOTES — packages requiring manual setup (run after pip install -r)
# ══════════════════════════════════════════════════════════════════════════════

# 1. OpenAI CLIP
#    Must use --no-build-isolation due to setuptools compatibility:
#    pip install git+https://github.com/openai/CLIP.git@dcba3cb2e2827b402d2701e7e1c7d9fed8a20ef1 --no-build-isolation

# 2. NSD Access (Natural Scenes Dataset utility library)
#    pip install git+https://github.com/tknapen/nsd_access.git

# 3. Stable Diffusion v1.4 — ldm (local editable install required)
#    Must be cloned and installed as editable — pip git install does NOT work:
#    mkdir -p codes/diffusion_sd1
#    git clone https://github.com/CompVis/stable-diffusion codes/diffusion_sd1/stable-diffusion
#    cd codes/diffusion_sd1/stable-diffusion && pip install -e . && cd ../../..
#
#    SD weights (sd-v1-4.ckpt) are downloaded automatically via gated HuggingFace access.
#    See README.md for token setup instructions.

# 4. Taming Transformers (required by ldm — must also be local editable install)
#    pip git install does NOT work reliably. Clone locally:
#    git clone https://github.com/CompVis/taming-transformers codes/diffusion_sd1/taming-transformers
#    cd codes/diffusion_sd1/taming-transformers && pip install -e . && cd ../../..
#
#    After installing, if import still fails, manually create __init__.py:
#    Create empty file at: codes/diffusion_sd1/taming-transformers/taming/__init__.py
